import { ThumbDirective } from './thumb.directive';

describe('ThumbDirective', () => {
  it('should create an instance', () => {
    const directive = new ThumbDirective();
    expect(directive).toBeTruthy();
  });
});
